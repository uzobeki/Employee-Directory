import React from "react";
import "./style.css"

const Header = () => {
    return (
        <header>
            <h1>Employee Directory</h1>
            <p>Click on a column headers to filter by heading or use the search bar to narrow results!</p>
        </header>
    );
};

export default Header;